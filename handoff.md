# Verity — Handoff / Living Status

> **Update this file after every meaningful action** (step/task done, blocker hit, decision made, open question resolved). A fresh agent must be able to resume from this file alone. Append to the log; don't silently rewrite history.

**Last updated:** 2026-06-21 — by: VERITY v3 PLAN 1 (discovery funnel) COMPLETE + LIVE-VERIFIED on branch `verity-v3-plan1-discovery` (106/106 tests, tsc clean, offline `--fixture` green). Live integration test against real data PROVED the funnel discovers Mantle RWAs beyond USDY/mUSD (Firecrawl found MI4/Securitize + the xStocks suite) and auto-verifies the issuer-confirmed ones (USDY+mUSD via Ondo `/addresses` ∩ on-chain `eth_getCode`) while quarantining the rest (MI4, xStocks). Task 7 was added post-test to fix a live wiring gap (raw `fetch` couldn't read JS-rendered issuer docs). The stubbed discovery (`fetchCandidates → []`, `lookup → null`) is replaced by a real funnel: candidate metadata (`claimedAddress`/`sourceUrl`) + `issuer-official` source role → strict issuer-address matcher → async issuer-official+on-chain resolver → async `matchOnchain` that auto-promotes issuer-confirmed candidates → live Firecrawl (candidates) + Etherscan V2 chainid=5000 (on-chain confirm) CLI wiring. Spec: `docs/superpowers/specs/2026-06-20-verity-v3-multisource-discovery-verification-narrative.md` (decisions: Both deliverables; agreement-tiered Dune+Nansen verify w/ Etherscan fallback; baked Dune charts; broadened report scope; auto-verify on issuer-source ∩ on-chain). Plans 2 (multi-provider verify + cross-check) and 3 (narrative + Dune charts + flagship run) still TODO.

**Earlier (2026-06-20):** FRESH LIVE RUN + RE-ATTESTATION done (resolves the stale-hash follow-up). New real ValidationRegistry tx `0x3b33774fdc5c63e517d61cdd48601d91dabd66ddf8647c3d123653e7dd4d8600` on Mantle mainnet, status success, block 96916516, gas 176747. **Verified on-chain: the published PDF's keccak256 `0xe8e2387ee8a4b03f1f602a2aa42c9283254304eb0671386ce922852bbfd66d24` IS the on-chain requestHash** (confirmed present in tx calldata) → recomputable-hash guarantee now holds in production with the single-render fix (commit 5f98888). examples PDF committed + pushed so the requestURI raw-GitHub bytes match the anchor. Prior tx 0xcc51… is now superseded.

---

## TL;DR (read this first)

- **What:** Verity, a verification-first onchain-finance research agent. Mantle Research Challenge, Track 2. Deadline **July 3, 2026**.
- **Where we are:** Build complete + PRODUCTION LIVE RUN done. 45/45 tests, tsc clean. Repo PUBLIC at https://github.com/damli40/Verity. On-chain identity REGISTERED (agentId 134). Real Dune query `7749240` (public, re-runnable) backs a non-fixture live run; gpt-5 synth + judge + deterministic gate all PASS against real on-chain data; the report's honest verification-first finding is **"No — USDY/mUSD adoption on Mantle did NOT accelerate in Q2 2026; on-chain supply contradicts the bullish 27% RWA-growth headline."** REAL ValidationRegistry attestation landed on Mantle mainnet: tx `0xcc51a9016a69234f72ba1189bb88d4e8ba7a40a020fc8c1eee4d3443e616d63d` (block 96827854, status 0x1).
- **Real data anchors:** Allowlist = USDY `0x5bE26527e817998A7206475496fDE1E68957c5A6` + mUSD `0xab575258d37EaA5C8956EfABe71F4eE8F6397cF3` (Ondo official docs, docs.ondo.finance/addresses). Dune query 7749240 = quarterly net-mint / end-of-quarter supply / transfer volume+count for USDY & mUSD on Mantle from `erc20_mantle.evt_transfer`. Live findings: USDY supply $26.33M (Q1) → $25.90M (Q2-to-date), net mint +474.5K → −431.8K; mUSD ~$57.7K flat.
- **Next action (submission only — code/chain DONE):** Manual submission: X thread tagging @Mantle_Official (+ follow/like/share article), join Mantle creators Discord, submit form with wallet address `0x36599B0286D3c32F934BC8877eeA839D88f3AFFC`.
- **Run notes:** scripts read process.env directly → always run with `node --env-file=.env --import tsx <script>`. Cheap LLM test loop (no Dune/Exa): `VERITY_FIXTURE_LIVE_LLM=1 node --env-file=.env --import tsx src/cli.ts --fixture` (use `VERITY_SYNTH_MODEL=gpt-5-2025-08-07` for a passing run; weaker synth models may be rejected by the judge).
- **Confirmed addresses (Mantle mainnet, chainId 5000):** ValidationRegistry `0x8004Cc8439f36fd5F9F049D9fF86523Df6dAAB58`, Identity `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432`. Agent: agentId 134, validatorAddress `0x36599B0286D3c32F934BC8877eeA839D88f3AFFC` (in `.env`).
- **Spec:** `docs/superpowers/specs/2026-06-17-verity-onchain-research-agent-design.md`
- **Plan:** `docs/superpowers/plans/2026-06-17-verity-onchain-research-agent.md`

## How to resume

1. Read `CLAUDE.md` (esp. §5 Cardinal Rule and §6 Discipline).
2. Read the plan. Find the first unchecked task below.
3. Execute it via TDD (failing test → fail → impl → pass → commit). Update this file when done.

## Task status

| # | Task | Status |
|---|------|--------|
| 0 | Repo scaffold | ☑ done |
| 1 | Core domain types | ☑ done |
| 2 | Address allowlist + loader | ☑ done |
| 3 | **Deterministic provenance checker (spine)** | ☑ done |
| 4 | **Confidence scorer** | ☑ done |
| 5 | Dune client | ☑ done |
| 6 | onchain-finance-scout (allowlist) | ☑ done |
| 7 | web-scout (Exa) | ☑ done |
| 8 | Synthesizer (Claude) | ☑ done |
| 9 | LLM-as-judge | ☑ done |
| 10 | Verification gate | ☑ done |
| 11 | Cost transparency | ☑ done |
| 12 | Telemetry (PostHog) | ☑ done |
| 13 | Report builder (PDF) | ☑ done |
| 14 | ERC-8004 attestation | ☑ done |
| 15 | Operator orchestrator | ☑ done |
| 16 | CLI + cached fixture run | ☑ done |
| 17 | SKILL.md + README + submission | ☑ done |

Legend: ☐ not started · ◐ in progress · ☑ done

## Open questions / decisions pending

- [ ] **Repo language** — locked to **TypeScript** (Node/ESM, tsx, vitest). Revisit only if blocked.
- [ ] **Mantle ERC-8004 Validation Registry address** — IdentityRegistry (`0x8004A169FB4a3325136EB29fA0ceB6D2e539a432`) and ReputationRegistry (`0x8004BAa17C55a88189AE136b182e5fdA19dE9b63`) are confirmed from the official erc-8004-contracts README for Mantle Mainnet. The **ValidationRegistry address is NOT listed** in the README for any chain. Must be located and verified on `mantlescan.xyz` before any mainnet tx. Set as `ERC8004_VALIDATION_REGISTRY` in `.env` once confirmed. ABI is confirmed from `abis/ValidationRegistry.json` in the same repo — real signature is `validationRequest(address validatorAddress, uint256 agentId, string requestURI, bytes32 requestHash)` (differs from original spec placeholder).
- [ ] **Fixture synthesis without `ANTHROPIC_API_KEY`** — decide in Task 16 whether to check in a `fixtures/report.json` so the offline demo runs with zero API keys.

## Decisions log (newest first)

- 2026-06-17 — Attestation target: **Mantle mainnet via the existing Validation Registry** (reversed earlier testnet-only call; Mantle deployed ERC-8004 on 2026-02-16, gas is cheap, more credible).
- 2026-06-17 — Cost feature **simplified** to estimated/actual compute + time-saved (dropped +50% margin & mock invoice).
- 2026-06-17 — v1 uses a **hand-verified address allowlist**; dynamic resolution is roadmap (avoids the resolution-hallucination hole).
- 2026-06-17 — Verification gate split: **deterministic checker (hard, programmatic)** + **configurable LLM judge (qualitative only)**.
- 2026-06-17 — Live example reframed to a **claim that must be proven**: "Did Mantle's RWA growth actually accelerate in Q2 2026, and are tokenized-equity adoption claims supported onchain?"
- 2026-06-17 — Added **cached fixture run** so the demo always renders offline.

## Known minor follow-ups (accepted, non-blocking)

- `evals/` directory is empty — eval harness is roadmap, not blocking submission.
- `now` parameter in `runResearch` is typed as `string` with a documented `void` note in the plan; left as-is per surgical-change rule.
- `ERC8004_IDENTITY_REGISTRY` is used only in the live `attest.ts` setup path; fixture path never reaches it.

## Blockers

- None yet.

## Progress log (append-only)

- 2026-06-21 — **CoinGecko folded into the discovery funnel (spec source 0).** Evaluated CoinGecko Demo API as a discovery candidate source. **Verified (Demo key, capability confirmed):** `/coins/categories/list` has `real-world-assets-rwa` (+ `rwa-protocol`, `real-estate`); a coin's `platforms.mantle` gives the canonical Mantle address (Ondo USDY → `0x5be2…c5a6`, matches our verified addr). Cross-query `/coins/markets?category=real-world-assets-rwa` ∩ `/coins/list?include_platform=true` filtered to `platforms.mantle` → 10 Mantle RWA coins with **distinct, correct per-token addresses** (USDY + full xStocks suite Tesla/NVIDIA/Apple/Amazon/Meta/Microsoft/Alphabet/SpaceX) — far cleaner than the rwa.xyz Firecrawl scrape (which collapsed all xStocks onto one address). Free Demo tier, REST + `x-cg-demo-api-key`, pipeline-callable. Caveats: CoinGecko RWA tagging carries noise (bridged LINK tagged RWA → would quarantine, no issuer-official entry — Cardinal Rule absorbs it) and is mcap-ranked/non-exhaustive (mUSD/MI4 absent from page 1, so it complements rwa.xyz/DefiLlama, not replaces). **Decision:** CoinGecko = **primary discovery candidate source** (spec Stage-1 source 0). **Follow-up (quick win, before flagship run):** add a `coingeckoRwaCandidates(json)` pure mapper + CLI `fetchCandidates` adapter with `COINGECKO_API_KEY` (Plan 1 same-shaped adapter; does NOT bypass issuer-official ∩ on-chain for auto-verify — CoinGecko's address is a candidate `claimedAddress`, not a trust anchor). Spec updated (Stage-1 source 0 + Plan 1 follow-up note); not yet wired in code.

- 2026-06-21 — **ORMI 0xGraph FOLDED INTO PLAN 2 (spec updated, commit pending).** Evaluated Ormi Labs as an additional on-chain provider. **Verified via their docs:** Ormi's REST **0xAPI is NOT on Mantle yet** ("Coming soon" — supported chains are Apechain/Arbitrum/Avalanche/Base/BSC/Ethereum/Plasma/Polygon/Somnia), but **0xGraph subgraphs ARE on Mantle** — Dedicated (Public) env at `https://subgraph.mantle.xyz/` (+ Mantle Sepolia). Decision (spec row 7): use **Ormi 0xGraph as the preferred second on-chain source** for cross-verification — deploy a no-code ERC-20 subgraph per verified RWA (keyed by the discovered address) → reproducible GraphQL query (same recomputable-trust property as a Dune query ID) for supply/transfers/holders; plain HTTP/GraphQL so the autonomous pipeline can call it (unlike the Dune/Nansen MCPs). Cross-check second-source preference: **Dune+Ormi ▸ Dune+Nansen ▸ Dune+Etherscan(confirm-only)**. Etherscan demoted to last-resort `eth_getCode`/supply confirm. Spec edits: ProvenanceRef union += `subgraph`; `ormi-scout` added; `onchain-checker` covers subgraph|nansen|etherscan; data-flow/testing/Plan-2 decomposition updated. **RESUME NEXT SESSION → write + execute Plan 2** (multi-provider verification + cross-check) from the updated spec; first build step deploys ERC-20 subgraphs on Ormi for USDY/mUSD/MI4. Note: Nansen MCP re-keyed but needs a session restart to bind before its live data is usable.

- 2026-06-21 — **PLAN 1 TASK 7 (live wiring fix) + LIVE INTEGRATION TEST.** A pre-merge live test exposed that the discovery funnel discovered real candidates but auto-verified NONE: issuer confirmation used a raw `fetch()` against the bare issuer-official domain root, but issuer docs are JS-rendered (`docs.ondo.finance` → 316KB shell, no addresses) and the addresses live at a sub-page (`/addresses`). **Task 7 (3a73cba, reviewed clean):** added `SourceAllowlistEntry.addressesUrl?`; `issuerOfficialUrl(issuer,list)` (addresses page if set, else `https://{domain}`); `makeLookup` now resolves the confirmation URL via `issuerOfficialUrl`; Ondo entry → `addressesUrl: https://docs.ondo.finance/addresses`; CLI `fetchText` swapped from raw `fetch` to a JS-rendering Firecrawl scrape (`scrapeText`, no-ops without `FIRECRAWL_API_KEY`). 106/106 tests, tsc clean, offline `--fixture` green. **LIVE PROOF (real Firecrawl + Ondo /addresses scrape + Mantle RPC eth_getCode):** candidates [MI4, Tesla xStock, USDY, mUSD] → VERIFIED {USDY, mUSD} (issuer-official ∩ on-chain), QUARANTINED {MI4 (Securitize has no addressesUrl yet), Tesla xStock (xStocks not an allowlisted issuer)}. Cardinal Rule upheld: only issuer-confirmed addresses auto-verify. Open Minor (final-review triage): `makeLookup` JSDoc still says "domain" (should say "URL"); MI4/xStocks need issuer-official addressesUrl entries to verify (data, not code). NOTE: `--fixture` renders to the attested `examples/mantle-rwa-q2-2026.pdf` path and clobbers it — restore with `git checkout` after any fixture run; pointing fixture output elsewhere is a deferred cleanup.

- 2026-06-21 — **VERITY v3 PLAN 1 (Discovery Funnel) COMPLETE** on branch `verity-v3-plan1-discovery` (6 TDD tasks, subagent-driven; 103/103 tests, was 88; tsc clean; offline `--fixture` `passed:true`). Plan: `docs/superpowers/plans/2026-06-20-verity-v3-plan1-discovery-funnel.md`. Closes the "discovery is stubbed; only USDY/mUSD are hand-typed" gap. Built: **(T1, 986b7f2)** `RwaCandidate`/`RawCandidate` gain `claimedAddress?`+`sourceUrl?`; `SourceRole` gains `"issuer-official"`; `SourceAllowlistEntry` gains `issuer?`; `data/source-allowlist.json` adds issuer-official Ondo/Securitize/Maple; `parseCandidates` passthrough. **(T2, 199f9aa)** pure `matchIssuerAddress(claimedAddress, issuerPageText)` in `src/discovery/resolve-address.ts` — the Cardinal-Rule core: an address is returned only when the issuer's own page literally confirms it, else null. **(T3, 23dbf1e)** `issuerOfficialDomain(issuer, list)` in source-allowlist.ts + async `makeLookup({list, fetchText, confirmOnchain})` resolver (`ResolvedAddress` type) — resolve issuer-official domain → fetch → matchIssuerAddress → on-chain confirm; any failure ⇒ null. **(T4, e0d32b0)** `matchOnchain` is now async, lookup `(c)=>Promise<ResolvedAddress|null>`; non-null ⇒ verified (reuse existing allowlist entry by case-insensitive address, else synthesize `status:"verified"`,`chainId:5000` entry — the deliberate auto-promote per spec decision 5, gated on issuer-source ∩ on-chain, never a bare registry claim); null ⇒ quarantine. **(T5, 3cbc080)** pure `toRawCandidate(row, sourceUrl)` in `src/discovery/sources.ts` + live `discover` wiring in `cli.ts`: Firecrawl `/v1/extract` (`extractRwaRows`) for candidates → `toRawCandidate` → `runRegistryScout` → `makeLookup` (fetch + `confirmErc20OnMantle` = Etherscan V2 `chainid=5000` `eth_getCode`, result≠"0x") → `matchOnchain`; both helpers return `[]`/`false` when their key is unset, and the fixture path never reaches them (offline-safe). `.env.example` += `FIRECRAWL_API_KEY`/`ETHERSCAN_API_KEY`. Each task fresh-implementer + spec/quality review, all clean. **Minor follow-ups for final review (in `.superpowers/sdd/progress.md`):** (a) `matchIssuerAddress` no-claim path returns `found[0]` raw-case (duplicate-different-case unhandled; benign, downstream compares lowercased); (b) synthesized `AllowlistEntry.address` may be page-casing not EIP-55 (cosmetic; matchOnchain/Etherscan are case-insensitive). **Accepted deferrals (spec, not bugs):** additional candidate sources (DefiLlama protocol list, Nansen screener, Dune issuer-deployer enumeration) are same-shaped injected adapters; Nansen/Etherscan as *verification* providers + cross-check = Plan 2; live discovery correctness exercised end-to-end in Plan 3's flagship run. Branch not yet merged to main.

- 2026-06-20 — **FRESH LIVE RUN + RE-ATTESTATION (closes the stale-hash follow-up from the post-review fixes).** Ran the full non-fixture pipeline (`node --env-file=.env --import tsx src/cli.ts`): real Dune (query 7749240) + Exa + OpenAI synth/judge → gate PASSED → rendered deck ONCE → hashed those exact bytes → real ValidationRegistry tx on Mantle mainnet. Result `{passed:true, pdfPath, attestationTx:"0x3b33774fdc5c63e517d61cdd48601d91dabd66ddf8647c3d123653e7dd4d8600", discovered:{verified:[],quarantined:[]}}`. **On-chain verification (viem against MANTLE_RPC_URL): receipt status success, block 96916516, gas 176747, to=ValidationRegistry 0x8004Cc84…; the published PDF's keccak256 `0xe8e2387ee8a4b03f1f602a2aa42c9283254304eb0671386ce922852bbfd66d24` is present in the tx calldata == the on-chain requestHash.** This proves the single-render fix (5f98888) end-to-end in production: recompute keccak256 of the published examples PDF and it matches the chain. examples PDF committed + pushed so the requestURI (raw.githubusercontent.com/damli40/Verity/main/examples/mantle-rwa-q2-2026.pdf) serves the attested bytes. Prior tx 0xcc51…e616d63d (block 96827854) is superseded. Discovery returned empty (live per-registry parsers still stubbed → quarantine-by-default, expected). Earlier offline proof this session: keccak256(published)==attested simulated hash; HTML contains requestHash + agentId anchor, no embedded `tx 0x`; offline cost shows actual $0.00 (no LLM call).

- 2026-06-20 — **POST-REVIEW FIXES (commit 5f98888).** Self-review of v2 (via superpowers requesting-code-review lens, done inline — not the receiving-code-review skill, which is for responding to feedback) surfaced 3 substantive findings + minors; all fixed TDD-style. **(Critical) Recomputable-hash break:** operator rendered a "pending"-tx draft → hashed+attested THAT → re-rendered with the real tx embedded (render-deck.ts:58), so the published PDF's keccak256 ≠ on-chain requestHash — directly violating CLAUDE.md §5. Fix: render ONCE, hash+attest those exact bytes, never embed the tx. `DeckMeta.attestationTx` removed; added optional `anchor {agentId,registry,chain}` (known pre-render). Deck attestation slide now shows a "recompute keccak256 and match the ValidationRegistry record" instruction + the anchor, not the tx. operator.ts no longer double-renders. **(Important) Fabricated cost:** `actualCost` used hardcoded token counts (9_000/1_800). Fix: `complete()` returns `{text,tokens}`; `synthesize()` returns `{report,tokens}` (SynthesisResult); judge tokens thread through `GateResult.judgeTokens`; operator's `actualUsd` = measured tokens (offline fixture = $0, honest). estimateUsd stays a labeled upfront estimate. **(Important) Flat-constant confidence:** every claim got `{0.9,0.85,0.9}`. New pure `deriveSignals(claim,dune,scrapes,asOf)` in confidence.ts derives sourceQuality (all-dune=1 / mixed=0.85 / scrape=0.7 / none=0.4), sourceAgreement (≥2 distinct sources=0.9), freshness (linear decay over 45d from oldest backing source), onchainVerified — 4 new tests. **(Minor)** deriveTier honors `forwardLooking`; removed dead doughnut-legend `<rect>`; fixed stale generate-pdf JSDoc. Touched: llm, synthesizer, llm-judge, gate, confidence(+test), tier, chart-svg, generate-pdf, slides(+test), render-deck(+test), operator(+test), cli. 88/88 (was 84), tsc clean, `--fixture` renders 176KB attested deck. **⚠️ FOLLOW-UP: the existing live tx 0xcc51… anchored the OLD pending-draft hash; do a fresh live run + re-attestation so keccak256(published examples PDF) == on-chain requestHash before the submission leans on the recomputable-hash claim.** Also still open (pre-existing): synth doesn't emit `claim.category` (all→"other"→single deck section); `chart.js` unused dep.

- 2026-06-20 — **VERITY v2 PLAN 3 (Landscape Deck Engine) DONE.** Plan: `docs/superpowers/plans/2026-06-20-verity-v2-plan3-deck-engine.md` (6 TDD tasks + 1 mid-task pivot). Built: `src/types.ts` (Slide union, ChartSpec, RWA_CATEGORIES order); `src/report/theme.ts` (design tokens, landscape @page CSS, tier badge colors Verified=green #1a7f4b / Corroborated=amber #b8860b / Forward-looking=grey #6b7280, TIER_LABELS, two-column flex content layout w/ FIXED mm widths); `src/report/charts.ts` (`selectChart` → line for temporal labels / doughnut for shares / bar otherwise, null <2 metrics); `src/report/slides.ts` (`buildDeck(report,meta):Slide[]` — cover→toc(Roman numerals+page ranges)→per-category divider+content→appendix; groups by category w/ "other" fallback; captionOf/calloutOf/sourcesOf helpers); `src/report/render-deck.ts` (`renderDeck` pure HTML, replaces render-html.ts which was deleted; ReportMeta re-exported); `src/report/generate-pdf.ts` (landscape via preferCSSPageSize+@page). CLI swapped to renderDeck. 84/84 tests, tsc clean, offline `--fixture` PASS → 9-page landscape deck **visually verified via pdftoppm** (cover gradient, TOC, divider, content slides w/ VERIFIED badges + SVG charts + Dune #44 captions + footers, Sources appendix w/ 4 re-runnable Dune URLs, attestation page). **KEY PIVOT (committed 0b71da5): Chart.js <canvas> does NOT rasterize in Chromium `page.pdf()` — embeds blank (pdfimages: 536B, x-ppi 0), confirmed via pdftoppm; live-canvas, canvas→PNG-img (data-URL also blanks), preferCSSPageSize toggles all failed. Replaced with pure inline-SVG chart renderer `src/report/chart-svg.ts` (line/bar/doughnut, no JS, no canvas, vector → renders reliably + keeps hash recomputable + render-deck stays pure). Also: % flex-basis collapses to 0 in paged media → use fixed mm widths.** Accepted follow-up: `chart.js` is now an UNUSED dependency in package.json (harmless, left installed; remove in a cleanup pass). Note for live runs: synthesizer doesn't yet set `claim.category` (all → "other" → single deck section) — categorized sections need the synth prompt to emit categories (future polish, not blocking).


- 2026-06-19 — **VERITY v2 PLAN 2 (Discovery + Operator/CLI Wiring) DONE.** Plan: `docs/superpowers/plans/2026-06-19-verity-v2-plan2-discovery-and-wiring.md` (5 TDD tasks, backward-compatible). Built: `src/discovery/registry-scout.ts` (`parseCandidates` Mantle-only normalize + `runRegistryScout` injected fetch; added `RwaCandidate` to types.ts); `src/discovery/match-onchain.ts` (`matchOnchain` → `{verified, quarantined}` via injected `lookup`, verified ONLY when resolved addr matches a `status:"verified"` allowlist entry, never auto-promotes); `src/scouts/scrape-scout.ts` (`captureScrapes` full-page text + scrapedAt for corroboration string-match, distinct from Exa snippet web scout). Wired operator: `ResearchInput` += optional `sourceAllowlist`, `ResearchDeps` += optional `scrape`/`discover`, `ResearchOutput` += `discovered`; scrapes+discovery captured in parallel with dune/web; `runGate` now threaded with `scrapes`+`sourceAllowlist`; after gate passes, `deriveTier` attached to every claim. Wired CLI both paths: loads `data/source-allowlist.json`; fixture serves cached `fx.scrapes`/`fx.discovered` (added to `fixtures/mantle-rwa-q2-2026.json`) + fixed stale `entities` SPCXx/InsightX → USDY/mUSD; live path reads `VERITY_SCRAPE_URLS` (+ documented `VERITY_QUERY_IDS`/`VERITY_SCRAPE_URLS` in `.env.example`), discovery quarantines-by-default (no per-registry parser yet, `lookup`→null). **70/70 tests, tsc clean, offline `--fixture` render green** → `{passed:true, pdfPath, simulated attestation, discovered:{quarantined:[MI4]}}`. v2 path now exercised end-to-end offline. Curated report.json still Dune-backed so gate pass/fail unchanged. Committed (registry-scout, match-onchain, scrape-scout, operator, cli — 5 commits). **Deferred (accepted):** `src/cache/` TTL (freshness already enforced in checker, YAGNI); live discovery per-registry parsers + on-chain resolver; Plan 3 deck engine.

- ⏭️ **NEXT: Plan 3 (deck engine)** — `src/report/theme.ts` (design tokens/CSS), `slides.ts` (`buildDeck(report,meta):Slide[]`, group by category, `Slide` union type), `render-deck.ts` (replaces render-html.ts: cover/TOC/dividers/content slides w/ tier badges Verified=green/Corroborated=amber/Forward-looking=grey + source captions + footer + page#s/sources appendix), `charts.ts` (chart-type by metric shape: time-series→line, categorical→bar, composition→doughnut), landscape `generate-pdf.ts`. `tier`/`category`/`discovered` data is now available to render. Delphi "State of Token Markets" PDF (~/Downloads) = quality bar. Programmatic visuals only (offline + recomputable hash). Plan 3 NOT yet written.


- 2026-06-17 — Brainstormed + wrote spec; revised after external review (verification engine first, deterministic spine, allowlist, mainnet attestation, confidence scores, simplified cost). Wrote 17-task implementation plan. Created `verity/`, `CLAUDE.md`, `handoff.md`. No source code yet.
- 2026-06-17 — Task 0 complete. Scaffold committed (SHA: 321cfd82d622ea5794b1d61b85f5c673296e5824). Dirs: src/ data/ evals/ fixtures/ examples/ posthog/ docs/superpowers/specs|plans/. Files: package.json, tsconfig.json, vitest.config.ts, .gitignore, .env.example, README.md. npm install: 106 packages (7 audit vulnerabilities, non-blocking). npx playwright install chromium: SUCCESS (Chrome 149 + headless shell downloaded). git identity set locally (Dami / demiladeakins@gmail.com).
- 2026-06-17 — Task 1 complete. Created `src/types.ts` with 10 core domain types: ProvenanceRef, Metric, ConfidenceSignals, Claim, Report, DuneResultRef, AllowlistEntry, CheckFailure, CheckResult. Verified with `npx tsc --noEmit` (no errors). Committed (SHA: c55ee9900501aae8ed2ab2bc15a6cab1967cbf63).
- 2026-06-17 — Task 2 complete. Created `data/allowlist.json` (2 PLACEHOLDER zero-address entries for SPCXx + InsightX, intentionally unverified). Created `src/allowlist.ts` (loadAllowlist + isAllowed). TDD: test failed (cannot find module), impl written, all 3 tests pass (case-insensitive match, reject unknown, load from file). Committed (SHA: 96887b6).
- 2026-06-17 — Task 3 complete. Created `src/verify/provenance-checker.ts` (checkProvenance — the verification spine). Created `src/verify/provenance-checker.test.ts`. TDD: test failed (cannot find module), impl written, 1 failing test diagnosed (freshness boundary: `>` should be `>=` so executedAt exactly 45 days before asOf is treated as stale), fixed, all 7 tests pass. Committed (SHA: 5653429). Next: Task 4 — Confidence scorer.
- 2026-06-17 — Task 4 complete. Created `src/verify/confidence.ts` (scoreConfidence — auditable per-claim 0..100 confidence score). Created `src/verify/confidence.test.ts`. TDD: test failed (cannot find module), impl written (weighted: 30% sourceQuality + 25% sourceAgreement + 20% freshness + 25% onchainVerified), all 3 tests pass (high confidence for perfect signals, lower for weak signals, clamped 0..100). Committed (SHA: f096b99). Next: Task 5 — Dune client.
- 2026-06-17 — Task 5 complete. Created `src/scouts/dune.test.ts` and `src/scouts/dune.ts`. TDD: test failed (cannot find module), impl written (shapeDuneResult pure shaper + getLatestDuneResults fetch), 1 test passes. Committed (SHA: 69de487). Next: Task 6 — onchain-finance-scout.
- 2026-06-17 — Task 6 complete. Created `src/scouts/onchain-finance-scout.test.ts` (2 tests: resolve allowlisted entities + reject unknowns). TDD: test failed (cannot find module), impl written (resolveTargets + runOnchainScout). All 2 tests pass. Committed (SHA: 049a1ce). Next: Task 7 — web-scout.
- 2026-06-17 — Task 7 complete. Created `src/scouts/web-scout.test.ts` and `src/scouts/web-scout.ts`. TDD: test failed (cannot find module), impl written (shapeExaResults pure shaper + runWebScout fetch to Exa API with 6 results, text content, 500-char snippet truncation). 1 test passes. Committed (SHA: e458bdf). Handoff updated. Next: Task 8 — synthesizer.
- 2026-06-17 — Task 8 complete. Created `src/synthesizer.test.ts` and `src/synthesizer.ts`. TDD: test failed (cannot find module), impl written (buildSynthesisPrompt pure prompt-builder + synthesize IO function calling Claude). 2 tests pass (prompt contains question/queryId/URLs/addresses + provenance/queryId instructions). `npx tsc --noEmit` clean. Committed (SHA: 6cccc9c). Next: Task 9 — LLM-as-judge.
- 2026-06-17 — Task 9 complete. Created `src/verify/llm-judge.test.ts` (2 tests: prompt builder + verdict JSON parser). TDD: test failed (cannot find module), impl written (buildJudgePrompt, parseJudgeVerdict, judge async function with configurable VERITY_JUDGE_MODEL env var defaulting to claude-haiku-4-5-20251001). All 2 tests pass. `npx tsc --noEmit` clean. Committed (SHA: ab6b875). Next: Task 10 — verification gate.
- 2026-06-17 — Task 10 complete. Created `src/verify/gate.ts` (runGate — deterministic-first hard gate with injected judgeFn) and `src/verify/gate.test.ts` (3 tests: deterministic short-circuit, full pass, judge rejection). TDD: test failed (cannot find module), impl written, all 3 tests pass. Full suite: 24/24 pass. `npx tsc --noEmit` clean. Committed (SHA: fdec0ee). Next: Task 11 — cost transparency.
- 2026-06-17 — Task 11 complete. Created `src/cost.ts` (TokenUsage interface, estimateCost + actualCost shared pure function at 15µ USD/synth token + 1µ USD/judge token, timeSavedHours returning 4 hours). Created `src/cost.test.ts` (3 tests: positive estimate, positive actual, positive hours-saved). TDD: test failed (module not found), impl written, all 3 tests pass. `npx tsc --noEmit` clean. Committed (SHA: e697151). Next: Task 12 — telemetry (PostHog).
- 2026-06-17 — Task 12 complete. Created `src/telemetry.test.ts` (1 test: captures run event with injected sink). Created `src/telemetry.ts` (RunMetrics interface, Sink interface with capture + shutdown, makeTelemetry factory, defaultSink with PostHog or no-op). TDD: test failed (module not found), impl written, 1 test passes. `npx tsc --noEmit` clean. Committed (SHA: 5b2942a). Next: Task 13 — report builder (PDF).
- 2026-06-17 — Task 14 complete. Created `src/attest-8004/hash.ts` (hashFile — keccak256 of file bytes via viem), `src/attest-8004/hash.test.ts` (1 test, TDD pass), `src/attest-8004/abi.ts` (real ValidationRegistry ABI from erc-8004-contracts repo — `validationRequest(address,uint256,string,bytes32)`, differs from spec placeholder), `src/attest-8004/attest.ts` (attest() IO function, Mantle mainnet via viem walletClient). ABI updated to match real deployed interface. ValidationRegistry Mantle address NOT in README — must confirm on mantlescan.xyz before live tx (see Open questions). `viem` exports `mantle` from `viem/chains` (confirmed v2.52.2). tsc clean, 30/30 tests pass. Committed (SHA: 681b090).
- 2026-06-17 — Task 13 complete. Created `src/report/render-html.test.ts` (1 test: question/claim/confidence/dune-query-id/attestation-tx/chart.js all present). Created `src/report/render-html.ts` (renderReportHtml — pure HTML string builder with Chart.js bar chart, claim table, re-runnable source links, cost + attestation sections). Created `src/report/generate-pdf.ts` (htmlToPdf — headless Chromium via Playwright, networkidle wait + 600ms chart paint delay). TDD: test failed (module not found), impl written, 1 test passes. Playwright smoke test: wrote 52,272-byte PDF successfully. Scratch file + PDF deleted. `npx tsc --noEmit` clean. Committed (SHA: 480d912). Next: Task 14 — ERC-8004 attestation.
- 2026-06-17 — Task 15 complete. Created `src/operator.test.ts` (2 tests: gate-pass → pdf+attest called; gate-fail → renderPdf+attest NOT called). Created `src/operator.ts` (runResearch — wires onchain+web scouts → synthesize → confidence scoring → runGate → conditional renderPdf+attest → telemetry). TDD: test failed (module not found), impl written, 2/2 tests pass. Full suite: 32/32 pass. `npx tsc --noEmit` clean. Committed (SHA: f55ed12). Next: Task 16 — CLI + cached fixture run.
- 2026-06-17 — Task 16 complete. Created `src/cli.ts` (live + fixture branches), `data/allowlist.fixture.json` (2 demo addresses), `fixtures/mantle-rwa-q2-2026.json` (cached scout outputs), `fixtures/report.json` (pre-synthesized report). Appended `VERITY_VALIDATOR_ADDRESS=` to `.env.example`. Added `!examples/mantle-rwa-q2-2026.pdf` gitignore exception. Fixture run (`npx tsx src/cli.ts --fixture`): `"passed": true`, PDF 84,858 bytes, `attestationTx: "simulated-0x1ebb56f76f20"`. `npx tsc --noEmit` clean. Full suite: 32/32 pass. Committed (SHA: b770d79). Next: Task 17 — SKILL.md + README + submission.
- 2026-06-18 — Task 17 complete. Verified all three documentation files exist with correct content: SKILL.md (frontmatter + overview), posthog/events.md (PostHog event schema), README.md (full pipeline with triple-backtick fences, no placeholder text). Updated handoff.md TL;DR + progress log. All files ready for submission. Committed (SHA: df80311).
- 2026-06-18 — Post-review fixes complete (SHA: c3441e3). Fix A: wired `resolveTargets` into `src/operator.ts` so only entity-resolved allowlisted addresses are passed to the synthesizer (full allowlist still goes to provenance checker/gate). Added 3rd operator test proving the wiring. Fix B: vendored chart.js UMD bundle (`chart.umd.js`) inline into `src/report/render-html.ts` via `readFileSync` — CDN dependency eliminated, offline render confirmed. chart.js path resolved via filesystem join from `import.meta.url` (package `exports` field blocks deep `require.resolve`). Fixture demo: `"passed": true`, PDF 69,275 bytes. 33/33 tests pass, `npx tsc --noEmit` clean. Accepted follow-ups noted above.
- 2026-06-18 — OPEN QUESTION CLOSED: Mantle ERC-8004 ValidationRegistry CONFIRMED at `0x8004Cc8439f36fd5F9F049D9fF86523Df6dAAB58` (chainId 5000). Verified live via eth_getCode against https://rpc.mantle.xyz (UUPS proxy bytecode `0x6080604052...`); Identity `0x8004A169...432` + Reputation `0x8004BAa1...b63` also live. Canonical MAINNET_ADDRESSES from erc-8004-contracts/scripts/addresses.ts (CREATE2 vanity, same across mainnet chains). On-chain `validationRequest(address,uint256,string,bytes32)` matches wired ABI. Baked into `.env.example` + `src/attest-8004/abi.ts`. Committed (SHA: f693d2d). Remaining open: register agent identity → agentId/validatorAddress; real allowlist + Dune IDs; manual submission.
- 2026-06-18 — Added ERC-8004 identity registration helper. `src/attest-8004/register.ts` (`registerAgent(agentURI)` + pure `pickAgentId` + CLI entry) calls IdentityRegistry `register(string)` on Mantle and reads the minted agentId from the `Registered` event; prints VERITY_AGENT_ID + VERITY_VALIDATOR_ADDRESS for `.env`. Added `identityRegistryAbi` to `abi.ts`, `VERITY_AGENT_URI` to `.env.example`, README run-step 4. 3 new tests (pickAgentId), 36/36 suite, tsc clean. Committed (SHA: see git log). Remaining open: run register once with a funded Mantle wallet; real allowlist + Dune IDs; manual submission.
- 2026-06-18 — Added OpenAI provider support for testing. New `src/llm.ts` (`complete()` routes by VERITY_LLM_PROVIDER=anthropic|openai; pure `selectedProvider()` tested, 3 tests). Refactored synthesizer + llm-judge to call `complete()` (pure builders/parsers unchanged). `VERITY_FIXTURE_LIVE_LLM=1` runs real synth+judge over cached fixture data (no Dune/Exa keys). Installed `openai`. Updated `.env.example` (VERITY_LLM_PROVIDER, OPENAI_API_KEY, VERITY_FIXTURE_LIVE_LLM) + README "Model providers". Fixed agent URI → github.com/damli40/Verity. GitHub repo description + 10 topics set via gh. 39/39 tests, tsc clean. Pushed to origin/main.
- 2026-06-18 — ERC-8004 identity REGISTERED on Mantle mainnet. agentId=134, owner/validatorAddress=0x36599B0286D3c32F934BC8877eeA839D88f3AFFC, tx 0x60ebc6848131ebc9632f478005490503fa8825680a1290b0b0de9279898d6930. VERITY_AGENT_ID + VERITY_VALIDATOR_ADDRESS written to .env (gitignored). Remaining open: real allowlist addresses + Dune query IDs for a live run; then a real ValidationRegistry attestation; manual submission (X thread, Discord, form). NOTE: register.ts/attest.ts read process.env directly — run with `node --env-file=.env --import tsx <script>` (Node does not auto-load .env).
- 2026-06-18 — Live-LLM test (VERITY_FIXTURE_LIVE_LLM=1, OpenAI: synth o4-mini / judge gpt-5) surfaced two crashes from schema drift (metric missing provenance; claim missing metrics array). Hardened: `normalizeReport()` at the synthesizer boundary coerces raw model JSON into a valid Report (claims/metrics arrays defaulted); provenance-checker now fails-closed on a metric with no provenance ("un-sourced figure"); operator uses optional chaining. Result: no crash; gate correctly returns passed:false ("un-sourced figure in claim text with no metric") — o4-mini emitted a number in prose without a metric. 43/43 tests, tsc clean. Curated offline demo (default --fixture) still renders the clean attested PDF. Committed + pushed.
- 2026-06-18 — Full OpenAI pipeline verified end-to-end. With gpt-5 as synth: deterministic gate PASSED (provenance-tagged claims matched Dune fixture cells); gpt-5 judge then returned passed:false with a correct qualitative critique (fixture has no Q1-vs-Q2 time series to prove "acceleration"). Fixes: judge maxTokens 1024→4096 + synth 4096→8192 (room for reasoning models); parseJudgeVerdict now fails-closed on empty/invalid output (no crash); synthesize throws a clear error if model returns no JSON. 45/45 tests. Both gate stages now exercised with OpenAI. To get a green (passed:true) live run + rendered PDF, enrich fixtures with a Q1 TVL datapoint + acceleration claim so the report can actually answer the question. Curated default --fixture demo still renders the clean attested PDF.
- 2026-06-18 — PRODUCTION LIVE RUN COMPLETE (all three open items closed). (1) Authored real, public, re-runnable Dune query **7749240** ("Verity — Ondo USDY/mUSD quarterly adoption on Mantle (RWA)") over `erc20_mantle.evt_transfer` for USDY+mUSD; executed (medium, 0.899 credits, 22 rows). (2) Replaced placeholder allowlist with **real Ondo-verified addresses** (USDY `0x5bE2…c5A6`, mUSD `0xab57…7cF3` from docs.ondo.finance/addresses) — abandoned the fictional SPCXx/InsightX narrative per Cardinal Rule §5 (web search found no such Mantle contracts). Updated cli.ts entities → ["USDY","mUSD"]; set requestURI to the public GitHub raw PDF URL; set VERITY_QUERY_IDS=7749240 + synth model gpt-5 in .env. (3) Validated end-to-end via a throwaway simulated-attest dry run (gate PASS, PDF honest: "No acceleration; contraction"), then ran the REAL non-fixture pipeline → **real ValidationRegistry tx 0xcc51a9016a69234f72ba1189bb88d4e8ba7a40a020fc8c1eee4d3443e616d63d on Mantle mainnet, confirmed status 0x1, block 96827854, gasUsed 210947, 1 log**. 45/45 tests, tsc clean. The verification-first thesis paid off: on-chain data contradicted the bullish Messari "27% RWA growth" headline, and the deterministic gate proved every cited number against query 7749240. Remaining: manual submission only.
- 2026-06-19 — VERITY v2 PLAN 1 (Verification Foundation) MERGED to main (merge 58cda05). Brainstormed + specced the v2 upgrade (Mantle RWA specialist): registry-cross-reference discovery w/ human-in-loop verify+quarantine, two-tier gate (Verified Dune / Corroborated allowlisted-scrape strict string-match / Forward-looking), global-vs-Mantle accuracy rule, 45-day TTL, landscape templated deck (programmatic visuals). Spec: docs/superpowers/specs/2026-06-18-verity-v2-mantle-rwa-specialist-design.md. Plans: docs/superpowers/plans/2026-06-18-verity-v2-plan1-verification-foundation.md (Plans 2 discovery + 3 deck still TODO). Plan 1 = 7 TDD tasks: types deltas; source-allowlist (data/source-allowlist.json + loader/hasRole); strict figure parser; checkScrapeMetric (Corroborated tier); global-label accuracy rule; deriveTier; runGate threading. 62/62 tests (was 45), tsc clean, offline --fixture render intact (backward-compatible). TESTED on REAL DefiLlama Mantle scrape (RWA Active Mcap $238.95m, corroborates rwa.xyz ~$241M): passes real figure→corroborated; rejects tampered value, off-by-$10k non-literal figure (strict, no tolerance), and unlabeled global figure. NOTE: new gate/discovery/deck NOT yet wired into live operator/CLI — that's Plans 2-3; a live cli run today still = v1 behavior.
- 2026-06-18 — GREEN: full live OpenAI run passes both gates + renders PDF. `VERITY_SYNTH_MODEL=gpt-5 node --env-file=.env --import tsx src/cli.ts --fixture` → {passed:true, pdfPath, simulated attestation}. Changes: enriched fixtures/mantle-rwa-q2-2026.json with a 4-quarter RWA TVL QoQ time series (q44) + equity Q1/Q2 (q45) so "acceleration" is answerable; enriched report.json (curated offline) with acceleration + equity-trend claims; strengthened buildSynthesisPrompt (explicit answer, period labels, interpretation, cover sub-questions, 4-6 claims, metric labels required); normalizeReport now normalizes per-metric fields (label/value/address); escapeHtml null-safe. tsc clean, tests green. Remaining: real Dune query IDs + allowlist addresses for production; manual submission (X thread, Discord, form).
